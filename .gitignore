.DS_Store
node_modules/
*.log

# рабочие копии агентов — в репозиторий им нельзя
.claude/

# сырые тайлы высот: качаются заново, в репозиторий им нельзя (сотни мегабайт)
data/dem-tiles/
# промежуточные каталоги сборки
data/chunks-tmp/
# сырьё сборки: качается заново, в репозиторий не кладём
data/osm-raw/
data/osm-raw.json
data/*-center.json
